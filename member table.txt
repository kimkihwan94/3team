CREATE TABLE member (
    user_num INT AUTO_INCREMENT PRIMARY KEY,   -- 회원번호, 기본 키
    id VARCHAR(50) NOT NULL UNIQUE,            -- 아이디, 유니크하고 필수
    name VARCHAR(100) NOT NULL,                -- 이름, 필수
    nickname VARCHAR(100) NOT NULL,            -- 닉네임, 필수
    password VARCHAR(255) NOT NULL,            -- 비밀번호, 필수
    birthdate DATE NOT NULL,                   -- 생년월일, 필수
    phone VARCHAR(20) NOT NULL,                -- 전화번호, 필수
    email VARCHAR(100) NOT NULL UNIQUE,        -- 이메일, 필수, 유니크
    reset_token VARCHAR(255)                   -- 비밀번호 재설정 토큰
);

<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<%@ page import="java.sql.*" %>
<%@ page import="Util.JDBCUtil" %>
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>회원가입 완료</title>
    <script>
        function showAlert(message, redirectUrl) {
            alert(message);
            if (redirectUrl) {
                location.href = redirectUrl;
            }
        }
    </script>
</head>
<body>
    <%
        Connection conn = null;
        PreparedStatement pstmt = null;
        boolean success = false;
        String errorMessage = null;

        try {
            // 데이터베이스 연결
            conn = JDBCUtil.getConnection();

            // 사용자 입력값 받기
            String name = request.getParameter("name");
            String id = request.getParameter("id");
            String password = request.getParameter("password");
            String birthdate = request.getParameter("birthdate");
            String phone = request.getParameter("phone");
            String email = request.getParameter("email");

            // SQL 쿼리 준비
            String sql = "INSERT INTO member (id, name, password, birthdate, phone, email) VALUES (?, ?, ?, ?, ?, ?)";
            pstmt = conn.prepareStatement(sql);
            pstmt.setString(1, id);
            pstmt.setString(2, name);
            pstmt.setString(3, password); // 비밀번호는 해시화하여 저장하는 것이 좋습니다.
            pstmt.setDate(4, java.sql.Date.valueOf(birthdate));
            pstmt.setString(5, phone);
            pstmt.setString(6, email);

            // 쿼리 실행
            int rowsAffected = pstmt.executeUpdate();
            if (rowsAffected > 0) {
                success = true;
            }
        } catch (SQLException e) {
            e.printStackTrace();
            errorMessage = "데이터베이스 오류: " + e.getMessage();
        } catch (Exception e) {
            e.printStackTrace();
            errorMessage = "알 수 없는 오류가 발생했습니다.";
        } finally {
            // 자원 해제
            JDBCUtil.close(pstmt);
            JDBCUtil.close(conn);
        }

        if (success) {
    %>
            <script>
                // 회원가입 성공 시 알림창을 띄우고 로그인 페이지로 리디렉션
                showAlert("회원가입이 완료되었습니다.", "../login/login.jsp");
            </script>
    <%
        } else {
    %>
            <script>
                // 회원가입 실패 시 알림창을 띄우고 페이지 새로고침
                showAlert("<%= errorMessage != null ? errorMessage : "회원가입에 실패하였습니다. 다시 시도해 주세요." %>");
            </script>
    <%
        }
    %>
</body>
</html>

Error Code: 1064. You have an error in your SQL syntax; check the manual that corresponds to your MySQL server version for the right syntax to use near ')' at line 11
